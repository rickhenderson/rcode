# Sample code for Debugging Practice
# R Programming Course Week 2

simpleLoop <- function(){
     reps = 10
     myVector = c(1, 2, "three", "4", 5, 6, 7, "eight", 9, "ten",15)
     
     # Basic For loop
     for (i in seq_along(myVector)) {
          print(myVector[x])
     }
     traceback()
}